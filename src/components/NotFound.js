import React from 'react';

//Steless component (only to display)
const NotFound = () => {
    // return jsx
    return (
        <h2 className="notFound">NOT FOUND !</h2>
    )
};

export default NotFound;